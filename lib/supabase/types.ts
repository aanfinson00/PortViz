// Database types — regenerate with `supabase gen types typescript` once a
// Supabase project is linked. For now this is a placeholder so the client
// factories compile.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
